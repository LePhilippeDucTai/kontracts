# PROGRESS — `kontract`

Mis à jour automatiquement par Claude Code à la fin de chaque jalon.
Statuts possibles : `TODO`, `IN_PROGRESS`, `DONE`.

**Trader MVP target** : Phase 1 complète (J1–J10) en 2–3 semaines.
**Ordre d'exécution strict** : J1 → J2 → J3 → J4 → J5 → J5b → J6 → J7 → J7b → J8a → J8 → J9c → J9 → J10 → J11 → ...

## Phase 1 : MVP Trader (semaines 1–3)

| Jalon | Titre | Statut | Tag git | Résumé décisions |
|-------|-------|--------|---------|------------------|
| J1 | AST | DONE | j1-ast | Enums `Contract`/`Observable`/`Condition` purs + serde JSON ; constructeurs DSL + ops surchargés (`+ - * / neg`, `max/min`, `ge/gt/le/lt`, `!`) ; round-trip vérifié sur 10 contrats |
| J2 | Observables | DONE | j2-observable | `Path` (grille + spots par actif) + `Observable::eval(path, t)` ; logique numérique isolée hors AST ; erreurs UnknownAsset/StepOutOfRange/InconsistentPath ; 8 tests (payoffs call/put, spread, nested) |
| J3 | GBM Simulateur | DONE | j3-gbm | `Gbm::simulate` → `Array2[n_paths,n_steps]` schéma log-normal exact ; rayon par trajectoire ; RNG ChaCha8 seedé par (seed,index) → reproductible indép. de l'ordre ; `simulate_paths` → `Vec<Path>` ; moments empiriques vs théorie OK (8 tests) |
| J4 | Compilateur | DONE | j4-compiler | [Opus + revue] `compile(&Contract)` → `Plan{assets, fixed_dates, horizon, needs_fine_grid}` ; barrière = condition dépendante du prix sous when/until/anytime ; `Plan::time_grid` (européen = dates seules, barrière = grille dense) ; 9 tests dont 5 contrats de référence |
| J5 | Pricer de base | DONE | j5-pricer | `price_gbm` compositionnel : réduction en flux `(montant, date)` par trajectoire, `scale` échantillonné à la date du flux, actualisation déterministe `e^{-rt}`, moyenne rayon ; `McConfig`/`PriceResult` ; or/until/anytime → `Unsupported` (J6) ; call EU vs BS < 1 % (9 tests) |
| J5b | MC Diagnostics | DONE | j5b-diagnostics | `PriceResult` enrichi : `sample_std`, `std_error = σ/√n`, IC 95 % (`ci95_low/high`), `n_paths` ; `paths_for_tolerance(tol)` = `(1.96σ/tol)²` ; IC contient le prix BS, scaling quadratique vérifié (5 tests) |
| J6 | Barrières | DONE | j6-barriers | [Opus + revue] `Condition::eval(path,t)` (couche numérique) ; `until` = knock-out (flux strictement antérieurs à 1ʳᵉ activation) ; `anytime` = first-touch (optimal → J17) ; `when` étendu aux conditions prix ; activation par path via `first_activation` ; `or` → Unsupported (J17) ; KO call vs Reiner-Rubinstein+BGK < 2 % (6 tests) |
| J7 | Greeks | DONE | j7-greeks | `greeks_gbm` bump-and-reprice avec common random numbers (graine constante → variance effondrée) ; Δ/Γ (diff. spot), Vega (diff. vol), Rho (diff. taux = drift+discount) ; `Greeks`/`BumpSizes` ; vs BS : Δ<1 %, Vega/Rho<2 %, Γ<5 % (5 tests) |
| J7b | Surfaces Greeks | DONE | j7b-surface | `greek_surface` : grille `(spot×vol)` de prix/δ/γ/ν évaluée en parallèle (rayon) ; `GreekSurface` (Array2) + export `to_csv` et `to_pgm` (heatmap grayscale sans dépendance) ; surfaces δ/ν vs BS (<2 %/<3 %), monotonie δ (5 tests) |
| J8a | API Python ergonomique | DONE | j8a-dsl | DSL fluide Rust : arithmétique observable⊕scalaire (2 sens), `.clip(floor)`, `observable * contract`/`f64 * contract` = scale, méthodes `.when/.until/.anytime/.and/.or/.give`, alias `s()`, constantes devises USD/EUR/GBP/JPY ; `@` Python → `when` mappé en J8 ; call fluide ≡ verbeux, 10 contrats en une ligne (5 tests) |
| J8 | Bindings PyO3 | DONE | j8-pyo3 | Bindings PyO3 0.21 : classes `Observable`/`Condition`/`Contract`/`GBM`/`PriceResult`/`Greeks` ; opérateurs Python (`- * / @ + ~`, comparaisons → Condition) ; `Contract.price()/greeks()` ; constructeurs `S/spot/one/zero/at/const_` + devises ; build maturin (venv) OK ; 6 tests pytest (import, fluide `@`, call vs BS <1 %, greeks, KO, portefeuille) |
| J9c | Batch pricing | DONE | j9c-batch | `price_on_paths` (éval sur trajectoires pré-simulées) + `price_batch_gbm` : grille unifiée (union des dates, fine si barrière), **simulation unique partagée**, éval parallèle rayon par contrat ; batch ≡ pricing individuel ; **100 contrats en 0,14 s en release** (< 500 ms) ; 4 tests |
| J9 | Produits validation | DONE | j9-products | Module `products` (catalogue d'expressions DSL, moteur agnostique) : ZC, call/put EU, forward, straddle, bull spread, digital cash-or-nothing, up/down-and-out ; tous validés vs formules fermées (9 tests). **Asian reporté** (pas d'observable d'agrégation temporelle), **swaption reportée** (taux stochastiques → J24) |
| J10 | Release | DONE | j10-release | CI GitHub Actions (fmt + clippy 2 modes + `cargo test --release` + build wheel & pytest) ; workflow release multi-OS (maturin-action → PyPI sur tag `v*`) ; **wheel release construite et installée dans un venv neuf** (import + pricing OK, call ATM 10,44) ; README mis à jour. Phase 1 (MVP Trader) terminée ✅ |

## Phase 2 : Modèles avancés (semaines 4–8)

| Jalon | Titre | Statut | Tag git | Résumé décisions |
|-------|-------|--------|---------|------------------|
| J11 | Simulator trait | DONE | j11-simulator | `Simulator` trait (`simulate`, `simulate_paths`, `asset_name`) ; implémentation `Gbm` sans modification logique ; `price_gbm`/`price_batch_gbm` acceptent `&dyn Simulator` ; pricer agnostique au modèle ; 91 tests verts, régression J1–J10 vérifiée ; déverrouille J12–J14 (Heston, Dupire, SABR, Rough Bergomi) |
| J12 | Heston + Dupire | DONE | j12-heston-dupire | `HestonSimulator` (Euler-Milstein 2D, corrélation ρ, plancher v≥0) + `DupireSimulator` (vol locale bilinéaire, formule Dupire complète avec terme rK∂C/∂K) ; `dupire_from_gbm_calls` extraction depuis grille (K,T) ; bug `row[k]=s` avant/après évolution corrigé (affectait Heston et Dupire) ; 8 tests : GBM limit σ_v=0 (<1%), GBM limit κ=100 (<1.5%), stats log-returns, ρ impact OTM, round-trip Dupire (<3%), ATM Dupire (<3%), dyn Simulator, reproductibilité ; 89+8=97 tests verts |
| J13 | SABR + Merton | DONE | j13-sabr-merton | `SABRSimulator` (CEV stochastique, Euler log-vol exact) + `MertonJumpSimulator` (Poisson composé + diffusion GBM) ; exports via lib.rs ; 6 tests verts : SABR stable ATM, SABR β=1/ν=0 limite GBM (<1 %), SABR impact ρ OTM (signe+magnitude), Merton λ=0 limite BS (<1 %), Merton vs formule fermée (<3 %), Merton sauts positifs → OTM plus cher ; correction clippy assign_op_pattern ; 103 tests totaux |
| J14 | Rough Bergomi | DONE | j14-rough-bergomi | [Opus + revue] `RoughBergomiSimulator` (Bayer-Friz-Gatheral) : log-variance pilotée par fBm `B^H` (Hurst `H<½`) généré **exactement** par Cholesky de la covariance fractale `½(s^{2H}+t^{2H}−|s−t|^{2H})`, factorisation calculée **une fois** par `simulate` et partagée (coût O(n²) amorti), `B = L·Z` par trajectoire ; `cholesky_lower` maison (pas de dépendance LAPACK, pivots planchés à 0 pour robustesse SPD) ; log-vol `log v_t = log v0 + ξB_t − ½ξ²t^{2H}` → **E[v_t]=v0 exact** (correction de convexité d'Itô) ; spot log-Euler corrélé à la dernière innovation `z_idx` du fBm (corrélation **atténuée mais correctement signée** — limitation documentée). `simulate_fbm` exposé pour inspection. 5 tests : trajectoires bien formées, recouvrement de Hurst via scaling de variance (<10 %), kurtosis en excès >0.5 (H=0.1), convergence MC 1/√n (call), forward variance plat E[v_T]≈v0 + signe du levier (ρ<0 → put OTM > call OTM). Revue Opus : APPROVE. 5 nouveaux tests (95+5=100 tests d'intégration Rust). |
| J15 | Réduction variance | DONE | j15-variance-reduction | `VarianceReductionConfig { use_antithetic, use_control_variate }` dans `McConfig` ; `simulate_antithetic_gbm` (innovations −Z) + `price_antithetic_on_paths` (estimateur par paire) → σ² ÷ 2 sur call EU ATM ; `price_control_variate_on_paths` (β optimal Cov/Var) → variance réduite sur call KO ; `black_scholes_call` (d1/d2, N via Abramowitz-Stegun) ; `Simulator::simulate_antithetic_paths` + `gbm_params()` (défaut None, surchargés par GBM) ; `present_value_pub` exposé publiquement (J15) ; backward-compatible (`None` → J1–J14 inchangé) ; 109 tests verts. |
| J16 | Quasi-MC | DONE | j16-quasi-mc | `SobolGbm` : GBM avec séquence de Sobol (bit-reversal + Van der Corput) pour convergence O(1/N) au lieu de O(1/√N) ; `SobolSimulator<T>` wrapper générique (placeholder pour extensibilité future) ; conversion U[0,1)→N(0,1) via approximation d'Acklam ; 4 tests : pricing call basique, pricing call vs GBM standard (écart <5%), tendance de convergence multi-N (all finite), barrière KO (valid price). 113 tests verts. |
| J17 | Américaines (LSM) | DONE | j17-lsm | [Opus + revue] `price_american_lsm` (Longstaff-Schwartz) : **aucune** primitive AST ajoutée — l'exercice américain est un **mode d'exécution** du pricer (comme MC vs EDP), fonction dédiée prenant le payoff exercé (`Contract` quelconque, ex. `scale(max(K−S,0), one)`) + `exercise_dates`. Backward induction : valeur réalisée transportée, régression polynomiale (base `{1,S,…,S^{n−1}}`, équations normales `AᵀAc=Aᵀy` résolues par Gauss + pivot partiel) sur les **paths ITM seulement** → estime la continuation, qui **ne sert qu'à la décision** d'exercice (évite le biais in-sample) ; actualisation pas-à-pas entre dates puis jusqu'à t=0 ; `cashflows_pub`/`value_at` rebasent le payoff **à la date d'exercice**. Agnostique au modèle (trait `Simulator`). `LsmConfig{n_basis}` (défaut 3). Fallbacks sûrs (ITM<n_basis ou système singulier → continuation pure). 6 tests : call US ≈ call EU BS (pas d'exercice anticipé sans dividende), put US > put EU, call/put US vs arbre CRR <1 %, sensibilité base n_basis=2 vs 3 <2 %, non-régression `price_gbm`. Revue Opus APPROVE-WITH-NITS (aucun bug). Correction de lints clippy préexistants J16 (toolchain 1.94 : imports inutilisés, parenthèses, groupement de chiffres dans `sobol_simulator.rs`). 113 tests J1–J16 inchangés + 6 = 119.|
| J18 | Multilevel MC | DONE | j18-mlmc | [Opus + revue] `price_mlmc`/`price_mlmc_detailed` (Giles 2008) : décomposition téléscopique `E[P_L]=E[P_0]+Σ E[P_ℓ−P_{ℓ−1}]` sur grilles `Δt=T/2^ℓ` ; **couplage brownien fin/grossier** via `Simulator::simulate_level_pair` (GBM & Heston) — incrément grossier = somme des deux fins (GBM réutilise `√dt_f` ; Heston applique `/√2` puis `√dt_c`, deux mises à l'échelle distinctes mais correctes) ; run pilote → `V_ℓ`, **allocation optimale de Giles** `N_ℓ∝√(V_ℓ/C_ℓ)` (`optimal_allocation`), run principal (graine décalée → échantillons disjoints non biaisés), agrégation `Q=Σμ_ℓ`, `Var=Σ V_ℓ/N_ℓ` ; pricer **agnostique au modèle** (même `Contract`, aucune primitive AST). GBM exact (sans biais de discrétisation → `V_ℓ≈0` pour ℓ≥1) ; décroissance de variance & O(ε⁻²) démontrés sur **Heston** (Euler, cas demandant). `MlmcConfig`/`MlmcResult`. 6 tests : call MLMC≈BS (GBM), décroissance géométrique `V_ℓ` (Heston), coût ∝ ε⁻² (ratio≈4 quand ε÷2), MLMC≈MC std Heston (<2 %), allocation ∝√(V_ℓ/C_ℓ), **économies >2× vs MC plat à L=8** (~3,3×). Revue Opus APPROVE-WITH-NITS (aucun bug ; nits traités : `level_paths`=pilote+principal cohérent, garde-longueur sur `optimal_allocation`). 119 tests J1–J17 inchangés + 6 = 125. |
| J21 | Lecteur données | DONE | — | |
| J21-fast | Calibration rapide (< 1 sec) | DONE | — | |

## Phase 3 : Risk Manager (optionnel)

| Jalon | Titre | Statut | Tag git | Résumé décisions |
|-------|-------|--------|---------|------------------|
| J19 | Crank-Nicolson 1D | DONE | j19-cn1d | `PdeSolver` via Crank-Nicolson FDM: backward-in-time Black-Scholes PDE, Thomas tridiagonal solve, PSOR for American options, Dirichlet BCs. Tests: ATM put (EU≤US✓), ITM put premium, convergence, spot/vol sensitivity. 6/6 tests ✓ |
| J20 | ADI 2D | DONE | j20-adi2d | `Pde2dSolver` via ADI splitting for 2D PDEs (Heston: spot S + variance v). S-sweep + v-sweep, each tridiagonal solve. Bilinear interpolation. Tests: grid creation, Heston call/put, spot/variance monotonicity. 5/5 tests ✓ |
| J22 | Optimiseur complet (CMA-ES) | DONE | j22-cmaes | [Opus + revue] `optimizer.rs` : **CMA-ES** générique (`cmaes_minimize`) **agnostique au domaine** — minimise un `Fn(&[f64])->f64` quelconque sur un pavé `Bounds`, sans gradient (robuste au bruit MC) et global (échappe aux minima locaux). Constantes de stratégie de Hansen (2016) : poids log-décroissants normalisés, `μ_eff`, `c_σ/d_σ/c_c/c_1/c_μ`, `χ_n` ; échantillonnage `x=m+σ·B·D·z` via eigendécomposition `jacobi_eigen` (ajoutée à `numerics.rs`, Jacobi symétrique, vecteurs propres en colonnes, seuil d'arrêt relatif à l'échelle) ; chemins d'évolution `p_σ`/`p_c` (avec `C^{-1/2}=B·D⁻¹·Bᵀ`), Heaviside `h_σ`, mise à jour covariance rang-1+rang-μ, pas global `σ←σ·exp((c_σ/d_σ)(‖p_σ‖/χ_n−1))`. Échantillonnage **séquentiel déterministe** (graine `mix(seed,gén)`) + évaluation **parallèle rayon** → reproductible bit-à-bit. Contraintes par projection (clamp) → `best_params` toujours admissible. Calibration `calibrate_{heston,sabr,merton}_cmaes` (`calibration.rs`) en **common random numbers** (objectif lisse) et **espace normalisé [0,1]ⁿ** (échelles paramètres hétérogènes). Revue Opus : 1 BUG corrigé (`d_σ` : terme `−1` manquant dans `max`, Hansen Eq.55), robustesse Jacobi renforcée ; reste des équations vérifiées correctes. 8 tests J22 : sphère/Rosenbrock recouvrés <1 %, bornes respectées, reproductibilité, round-trip prix Heston/SABR/Merton <0.5 %, round-trip vol GBM (bien posé) <1 %. Note d'identifiabilité documentée (round-trip prix = critère robuste ; round-trip paramétrique exact requiert une surface riche). 136 + 8 = 144 tests d'intégration, suite complète verte. |
| J23 | Backtesting | DONE | j23-backtest | Module `backtest` (couche d'orchestration au sommet, **aucune** primitive AST ajoutée) : (1) **PnL de réplication par couverture en delta** (`backtest_delta_hedge`) — vend l'option à son prix modèle, couvre en delta auto-financé le long de trajectoires tirées par le **simulateur du moteur**, agrège le PnL terminal ; PnL moyen ≈ 0 (couverture cohérente avec le modèle générateur) et écart-type ∝ √dt → **validation par PnL réalisé** ; (2) `model_vs_market` (biais, RMSE, erreur max/relative) — prix historiques vs modèle ; (3) `stability` (dérive, volatilité des variations jour-le-jour, saut max) ; (4) `historical_model_prices` rejoue le pricer MC sur une séquence d'observations en restant agnostique produit/modèle (constructeur `(Contract, Gbm)`). 7 tests : delta BS (réf N(d1) + parité), trajectoire plate, PnL moyen ≈ 0 (<3 % de la prime), erreur ↓ avec la fréquence, stats modèle/marché, stabilité (plat/rampe), replay moteur vs BS (<1 %). 144 + 7 = 151 tests d'intégration, suite complète verte. |

## Phase 4 : Extension future

| Jalon | Titre | Statut | Tag git | Résumé décisions |
|-------|-------|--------|---------|------------------|
| J24 | Taux stochastiques (Vasicek/HW) | DONE | j24-rates | [Opus + revue] Module `rates` : taux court stochastique **lève l'actualisation déterministe**. Trait `ShortRateModel` (simulation du taux court + zéro-coupon affine) ; `Vasicek` (transition gaussienne **exacte**, ZCB affine `A·e^{−Br}`, options sur ZC fermées, **swaption analytique par Jamshidian**) + `HullWhite` (extended Vasicek, courbe plate `e^{−r₀T}` repricée exactement). Swaption MC (actualisation par compte de capitalisation réalisé `exp(−∫r)`) vs analytique. **AST inchangé** : `price_under_short_rate` rejoue tout `Contract` en actualisation stochastique → ZCB `when(at(T),one)` retrouve `P(0,T)` ; swaption = produit de taux pur dans un moteur dédié (pas de cas spécial pricer). Revue Opus : **aucun bug** (8 groupes de formules vérifiés vs Brigo-Mercurio), 2 nits robustesse traités. 7 tests : moments du taux court vs théorie, ZCB stochastique vs analytique (<2e-3), **swaption payeuse/receveuse MC vs Jamshidian (<3 %)**, parité payeur−receveur = swap forward (<1e-9), HW courbe plate (<1e-10), symétrie ATM. 151 + 7 = 158 tests d'intégration, suite complète verte. |
| J25 | FX simple | DONE | j25-fx | Module `fx` : change multi-devise **sans toucher à l'AST**. (1) **Garman-Kohlhagen** (call/put = BS avec `q = r_f`) + `fx_forward` (parité des taux couverte) — l'option de change vanille est un call de l'algèbre sous `Gbm(μ = r_d − r_f)` actualisé à `r_d`, déjà couvert par le moteur ; on fournit la référence analytique. (2) **Quanto** (call/put) : se ramène à un GBM mono-actif à **drift ajusté** `r_f − ρ·σ_S·σ_X`, actualisé en domestique ; corrélation spot/FX captée par l'ajustement. (3) **`CorrelatedGbm2`** : simulateur **multi-actifs** (deux GBM corrélés, Cholesky 2×2) peuplant deux `Spot` par `Path` → un **composite** `max(S·X − K, 0)` s'écrit en DSL et price comme un BS sur le produit log-normal `U = S·X` (vol combinée `√(σ_S²+σ_X²+2ρσ_Sσ_X)`). 7 tests : parité GK (<1e-10), FX vanille moteur vs GK (<5e-3), forward, quanto moteur vs analytique (<5e-3), monotonie quanto en ρ, **composite deux-facteurs corrélés vs BS(U) (<2 %)**, recouvrement de ρ empirique (<0.02). Taux déterministes (couplage FX/taux stochastiques laissé à une extension). 158 + 7 = 165 tests d'intégration, suite complète verte. **Roadmap complète** ✅ |

## Durcissement production (post-J25)

| Chantier | Statut | Résumé |
|----------|--------|--------|
| PDE précision (J19/J20) | DONE | [Opus review] **J19 CN 1D** : 2 bugs de coefficients corrigés (diffusion 2× trop grande → `¼Δtσ²j²` ; signe du drift sur les diagonales **implicites** inversé) + condition de bord par **linéarité** (Γ=0) au lieu du gel de Dirichlet ⇒ call ATM vs BS **2,4 % → 0,001 %**. **J20 ADI 2D** : réécrit en **schéma de Douglas** (θ=½) avec **terme croisé** `ρσvS·∂²V/∂S∂v` explicite (ajout du champ `rho`), mêmes coefficients corrigés, bords par linéarité ⇒ call Heston ADI vs **MC Heston < 0,5 %** (était « <30 % »). Revue Opus : aucun bug, 2 notes de limite documentées. 11 tests PDE (dont nouveau ADI-vs-MC). |
| Tests & CI | DONE | Matrice CI **3 OS × Python 3.9–3.12** pour wheel+pytest + job **couverture** (`cargo-llvm-cov` → Codecov). **Tests de propriété** (`proptest`, `tests/properties.rs`) : round-trip JSON de l'AST, monotonie du call en spot (↑) et strike (↓), positivité, parité GK. **Benchmarks** (`criterion`, `benches/pricing.rs`) : batch pricing 100 contrats, calibration GBM. **Bug corrigé** : `serde_json` parsait les f64 à 1 ULP près (round-trip AST non exact) → feature `float_roundtrip` activée ⇒ sérialisation **sans perte**. |
| Bindings Python étendus | DONE | `Contract.price()` accepte désormais **tout modèle** (`GBM` + `Model` via `heston/sabr/merton/rough_bergomi/sobol_gbm`) ; ajout **dividende `q`** sur GBM, **réduction de variance** (`antithetic`/`control_variate`), `price_american` (LSM), `price_under_rates` (J24). Exposés : **produits** (`european_call`, `straddle`, …), **taux** (`vasicek`/`hull_white`/`Swaption`/`swaption_mc`/`vasicek_swaption_analytic`), **FX** (`garman_kohlhagen_call/put`, `fx_forward`, `quanto_call`), **calibration** (`implied_volatility`, `fit_gbm_volatility`). Stubs `.pyi` + `py.typed`. **Bug corrigé** : `market_data::norm_cdf` oubliait le facteur `1/√2` (vol implicite biaisée) → délègue au noyau central. 17 tests pytest (7 + 10), wheel construite + installée + verte. |

## Phase 5 : Extensions DSL (optionnel)

| Jalon | Titre | Statut | Tag git | Résumé décisions |
|-------|-------|--------|---------|------------------|
| J26 | Observables temporels | DONE | j26-temporal-obs | `Average{obs,from_year,to_year}`, `RunningMax(obs)`, `RunningMin(obs)` dans l'AST + `average/average_over/running_max/running_min` ; `eval` par fenêtre filtrée (ε=1e-12) + guard `from>to` ; `try_fold` pour RunningMax/Min ; compilateur : `needs_fine_grid=true` auto-détecté ; validé : Asian < vanilla (Jensen), lookback > vanilla (dominance), AM-GM, round-trip JSON, grille fine. **Limite** : valeur dépend de `steps_per_year` (biais discret). Revue Opus APPROVE-WITH-NITS (0 bug). 8 tests, 173 tests intégration verts. |
| J27 | Basket n-actifs | DONE | j27-basket | `src/multi_asset.rs` : `CorrelatedGbmN` — N GBM corrélés via Cholesky N×N, `simulate_paths` peuple N `Path` nommés, basket/spread en DSL pur. 5 tests (corrélation empirique, basket 3 actifs vs BS, spread vs Margrabe, ρ=1 dégénérescence, validation constructeur). Revue Opus APPROVE-WITH-NITS. 173 + 5 = 178 tests verts. |
| J28 | Produits structurés | TODO | — | |
| J29 | Bindings Python J26/J27 | TODO | — | |

## Journal d'implémentation

<!-- Chaque jalon complété ajoute une entrée : date | Jx | titre | décisions clés prises -->

- 2026-06-16 | J27 | Basket N-actifs (CorrelatedGbmN) | `src/multi_asset.rs` : `CorrelatedGbmN` — simulateur de N GBM corrélés via une matrice de corrélation N×N factorisée par Cholesky. **Décision d'architecture** : aucun nouveau combinateur AST — les payoffs basket (`(S1+S2+S3)/3`) et spread (`(S1−S2).clip(0.0)`) s'expriment entièrement en arithmétique DSL existante ; seul le simulateur est nouveau. Réutilise `GbmFactor` (depuis `src/fx.rs`), `cholesky_lower` (depuis `src/numerics.rs`), `mix(seed, i)` (depuis `src/simulator.rs`) et le support multi-actifs de `Path` via `.with_asset()`. **Mécanique** : par trajectoire (rayon), génère z ~ N(0, I_k) par pas de temps, calcule w = L·z (matvec triangulaire inférieur), puis applique le schéma GBM log-exact par actif (`S_{t+dt} = S_t * exp((μ−σ²/2)·dt + σ·√dt·w)`). Le `Path` est peuplé par `try_fold` en chaînant les `with_asset`. **Erreurs validées dans le constructeur** : < 2 actifs, matrice non N×N, volatilité négative. `simulate` (mono-actif) extrait le premier actif pour compatibilité avec `price_gbm`. **Revue Opus** : APPROVE-WITH-NITS — 0 bug ; nit principal traité (σ≥0 validé dans le constructeur, pas au moment de la simulation). 5 tests : corrélation empirique ρ=0.7 sur log-rendements (±0.02), basket equal-weight 3 actifs vs BS(σ_basket = σ√((1+2ρ)/3)) (<3 %), spread option (S1−S2)+ vs Margrabe `black_scholes_call(S1₀, S2₀, T, 0, σ_spread)` (<3 %), ρ=1 dégénérescence (basket = S1 à <2 %), validation constructeur (erreurs attendues). 173 + 5 = 178 tests d'intégration, suite complète verte.
- 2026-06-15 | J26 | Observables temporels (Asian, Lookback) | `src/ast.rs` : trois nouveaux variants de `Observable` — `Average{obs,from_year,to_year}`, `RunningMax(obs)`, `RunningMin(obs)` — plus quatre constructeurs DSL (`average`, `average_over`, `running_max`, `running_min`). **Décision d'architecture** : les observables temporels sont des **combinateurs purs de l'AST** (description seulement, pas de logique numérique) ; l'évaluation reste dans `observable.rs`. Un Asian call s'écrit `scale(average(spot("X")) - K, one(USD)).when(at(T))` sans aucun cas spécial dans le pricer : `Scale` évalue l'observable à la date du flux (`at(T)`), et à ce pas `average` remonte la grille pour calculer la moyenne de S_0…S_T. Un lookback call : `scale(running_max(spot("X")) - K, one(USD)).when(at(T))`. **Évaluation** : `Average` filtre les pas de la grille dans `[from_year±ε, to_year±ε]` (défauts : 0 et `t_now`) avec garde `from>to` → `MalformedContract` clair ; `RunningMax/RunningMin` par `try_fold` sur `0..=t` (propagation d'erreur immédiate, identité correcte ±∞). **Compilateur** : `walk_observable` étendu — les trois variants positionnent `needs_fine_grid=true` afin que la grille dense soit générée. **Limite documentée** : la valeur d'une option asiatique ou lookback dépend du nombre de pas (`steps_per_year`) car on moyenne/scanne tous les points de la grille simulée ; l'extension à des dates de fixing contractuelles explicites est laissée à un jalon futur. **Revue Opus** avant commit : APPROVE-WITH-NITS — 0 bug, 4 nits ; 2 traités (commentaire `t_now` doubles as bounds-check, garde `from>to` ajoutée dans `eval` avec message précis). 8 tests J26 (4 unitaires sur path concret, 2 MC, 1 JSON, 1 compilateur). 165 + 8 = 173 tests d'intégration, suite complète verte. Tag `j26-temporal-obs`.
- 2026-06-15 | J25 | FX simple (multi-devise, corrélation spot/FX) | `src/fx.rs` : ajoute le **change** à l'algèbre **sans modifier l'AST** ni introduire de cas spécial produit dans le pricer (mêmes invariants de séparation que le reste). **Décision d'architecture** : le FX n'introduit que (a) des **références analytiques** et (b) un **simulateur multi-actifs** ; les produits restent des expressions DSL. Trois briques : (1) **Garman-Kohlhagen** `garman_kohlhagen_call/put` (Black-Scholes avec rendement de dividende `q = r_f`) et `fx_forward` (`F = X₀e^{(r_d−r_f)t}`, parité des taux couverte) — une **option de change vanille** est un simple call de l'algèbre sur le taux `X` sous `Gbm(μ = r_d − r_f)` actualisé à `r_d`, donc **déjà priçable par le moteur existant** ; le test le vérifie vs GK (<5e-3). (2) **Quanto** `quanto_call/put` : option sur l'actif étranger réglée en domestique à FX fixe ; la corrélation spot/FX entre par l'**ajustement quanto du drift** `μ_q = r_f − q_S − ρσ_Sσ_X`, ce qui ramène le pricing à un **GBM mono-actif** à drift ajusté actualisé en domestique → réutilise le moteur (test moteur vs analytique <5e-3 + monotonie décroissante en ρ). (3) **`CorrelatedGbm2`** (+ `GbmFactor`) : **simulateur multi-actifs** — deux GBM corrélés (browniens couplés par Cholesky 2×2 `z_b = ρz_a + √(1−ρ²)w`) qui peuplent **deux sous-jacents par `Path`**, exploitant le support multi-actifs déjà prévu par le trait `Simulator` (override de `simulate_paths`). Cela permet d'exprimer en DSL un **composite (cross-currency)** `max(S_T·X_T − K, 0)` réglé en domestique, référençant `Spot("STOCK")·Spot("FX")` ; comme le produit de deux log-normaux corrélés est log-normal, la référence analytique est un Black-Scholes sur `U = S·X` de **volatilité combinée** `composite_vol = √(σ_S²+σ_X²+2ρσ_Sσ_X)` (test MC deux-facteurs vs BS(U) <2 %). Ajout d'un accesseur `Path::spot_series` (série complète d'un sous-jacent) pour l'interface mono-actif `simulate` du simulateur 2-facteurs. **Convention** : taux déterministes (le couplage FX ↔ taux stochastiques de J24 est laissé à une extension future, documenté). **Style fonctionnel** : analytique et agrégations par itérateurs ; seul le noyau d'évolution GBM par trajectoire garde une récurrence séquentielle (exception documentée comme dans `simulator.rs`). 7 tests J25 : parité put-call GK (<1e-10), FX vanille moteur vs GK (<5e-3), forward (parité des taux), quanto moteur vs analytique (<5e-3), monotonie quanto en ρ, composite deux-facteurs corrélés vs BS(U) (<2 %), recouvrement de la corrélation cible des log-rendements (<0.02). fmt + clippy (défaut **et** `--features python`) verts. 158 + 7 = 165 tests d'intégration, suite complète verte. Commit + tag `j25-fx`. **Roadmap MVP+extensions intégralement complétée** (J1–J25) ✅.
- 2026-06-15 | J24 | Taux stochastiques (Vasicek/Hull-White) | `src/rates.rs` : introduit un **taux court stochastique** et **lève la décision « discount déterministe jusqu'à J24 »**. **Décision d'architecture clé** : un taux stochastique est un **mode d'exécution** du pricer, pas une primitive — l'AST reste pur (aucun observable de taux ajouté). `price_under_short_rate(contract, model, cfg)` rejoue n'importe quel `Contract` en remplaçant l'actualisation déterministe `e^{−rt}` par le compte de capitalisation réalisé `D(t)=exp(−∫₀ᵗ r ds)` (trapèze sur grille fine forcée) ; une obligation zéro-coupon `when(at(T), one)` de l'algèbre retrouve ainsi `P(0,T)` analytique **sans changer le contrat** (même esprit que « EDP = alternative d'exécution »). Trait **`ShortRateModel`** (`simulate_short_rate` → `Array2[n_paths,grid]` rayon, `zero_bond` affine, `r0`/`discount_bond0`). **`Vasicek`** `dr=a(b−r)dt+σdW` : transition gaussienne **exacte** (mean `r·e^{−aΔ}+b(1−e^{−aΔ})`, var `σ²/2a·(1−e^{−2aΔ})`, pas d'Euler), ZCB `P=A·e^{−Br}` avec `B=(1−e^{−aτ})/a`, `lnA=(b−σ²/2a²)(B−τ)−σ²B²/4a` ; options européennes fermées sur ZC (`σ_P=σ√((1−e^{−2at₀})/2a)·B(t₀,S)`, formule de Black sur obligation) ; **swaption analytique par décomposition de Jamshidian** (payeur=Σcᵢ·put, receveur=Σcᵢ·call sur ZC de strikes `Xᵢ=P(T₀,Tᵢ;r*)`, `r*` racine de `ΣcᵢP=1` par bisection sur fonction décroissante). **`HullWhite`** (extended Vasicek) sur **courbe initiale plate** `P(0,T)=e^{−r₀T}` repricée exactement (`θ(t)=a·r₀+σ²/2a·(1−e^{−2at})`, ZCB relatif à la courbe), même trait. **Swaption MC** (`swaption_price_mc`) : simule `r` jusqu'à `T₀`, payoff `max(0,±(1−ΣcᵢP(T₀,Tᵢ;r_{T₀})))` actualisé par `D(T₀)` réalisé → validé vs Jamshidian. **Pourquoi un moteur de taux dédié pour la swaption** : son payoff dépend de prix d'obligations `P(T₀,Tᵢ)`, que l'algèbre actions/spot n'exprime pas (même limite assumée que l'asian faute d'observable d'agrégation) → pas de shoehorn dans l'AST. **Revue Opus** (séparée, avant commit) : a revérifié les 8 groupes de formules vs Brigo-Mercurio (transition, ZCB affine, options sur ZC, assignation payeur↔put/receveur↔call et monotonie de Jamshidian, payoff/actualisation MC, courbe plate HW, intégration trapézoïdale et cohérence d'indexation des flux, bisection à court-circuit) — **aucun bug** ; 2 nits de robustesse traités (garde grille vide dans `cumulative_discounts`, clarification du commentaire de drift HW). **Style fonctionnel** : analytique et agrégations par `map`/`fold`/`scan`/`windows` ; seuls les noyaux de simulation par trajectoire gardent une boucle séquentielle (récurrence, exception documentée comme dans `simulator.rs`). 7 tests J24 : moments empiriques du taux court vs théorie (<1e-3 mean, <5 % var), ZCB stochastique vs `P(0,T)` analytique (<2e-3) sur 4 maturités, **swaption payeuse MC vs Jamshidian <3 %**, swaption receveuse MC vs analytique <3 %, parité payeur−receveur = swap forward (<1e-9), HW courbe plate (analytique <1e-10 + MC <2e-3), symétrie ATM payeur≈receveur (<1e-6). fmt + clippy (défaut **et** `--features python`) verts. 151 + 7 = 158 tests d'intégration, suite complète verte. Commit + tag `j24-rates`. **Reste J25 (FX simple)** en extension future.
- 2026-06-15 | J23 | Backtesting | `src/backtest.rs` : couche de **validation de modèle** au sommet de l'architecture — orchestre le pricer/simulateur existants sans ajouter aucune primitive à l'AST ni aucun cas spécial produit (mêmes invariants de séparation que le reste du moteur). **Décision d'architecture** : le backtesting est un **orchestrateur**, pas une primitive ; il ne fait qu'appeler `price_gbm`/`simulate` et évaluer des payoffs analytiques de référence. **Trois diagnostics** alignés sur le critère du jalon (« prix historiques vs modèle, stability » → « validation PnL réel ») : (1) **couverture en delta auto-financée** (`delta_hedge_error` sur une trajectoire, `backtest_delta_hedge` sur un ensemble) — on encaisse la prime `V_0 = BS(S_0)`, on constitue `(cash, shares)`, on accumule les intérêts puis rééquilibre au delta analytique à chaque date, on liquide et règle le payoff à l'échéance ; les trajectoires « réalisées » sont **tirées par le simulateur GBM du moteur** au drift/σ du modèle, donc la couverture est cohérente avec la mesure génératrice → **PnL moyen ≈ 0** (test : <3 % de la prime sur 20 000 paths) et **écart-type ∝ √dt** (test : ↓ quand `n_steps` ↑) — c'est la *validation par PnL réalisé* ; (2) `model_vs_market` — biais signé, erreur absolue moyenne, RMSE, erreur max, erreur relative moyenne (observations de marché nul ignorées) ; (3) `stability` — moyenne/écart-type des valeurs + **variation jour-le-jour** (dérive `change_mean`, volatilité `change_std`, saut `max_abs_change`) pour juger la stabilité d'une série de paramètres calibrés ou d'erreurs de pricing. **Pont avec le moteur** : `historical_model_prices(n_obs, build, cfg)` rejoue le pricer MC sur une séquence d'observations, `build(i)` fournissant le `(Contract, Gbm)` tel qu'à la date `i` (maturité résiduelle décroissante) — **agnostique produit/modèle** ; évaluation parallèle rayon. `bs_delta` (call = N(d1), put = N(d1)−1, indicatrice ITM à l'échéance) ajouté et exposé. **Style fonctionnel** : tout l'état (cash/shares, sommes, variances) accumulé par `fold`/`map`/`windows`, aucune boucle `for`, propagation par `Result`. 7 tests J23 : delta BS (réf N(d1) à 1e-12 + parité call/put), trajectoire plate (PnL fini borné), PnL moyen ≈ 0, erreur ↓ avec la fréquence de rééquilibrage, stats modèle/marché (séries égales → 0, décalage constant → biais=RMSE=décalage, longueurs incompatibles → erreur), stabilité (plat → variations nulles ; rampe → dérive constante, volatilité nulle ; série <2 → erreur), bout-à-bout replay moteur MC vs Black-Scholes (<1 % d'erreur relative, décroissance de la valeur temps). fmt + clippy (défaut **et** `--features python`) verts. 144 + 7 = 151 tests d'intégration, suite complète verte (161 avec unit/lib). Commit + tag `j23-backtest`. **Phase 3 (Risk Manager) terminée** ✅ ; restent J24/J25 (extensions futures, taux stochastiques & FX).
- 2026-06-15 | J22 | Optimiseur complet (CMA-ES) | `src/optimizer.rs` : implémentation **CMA-ES** (Covariance Matrix Adaptation Evolution Strategy, Hansen 2016) comme optimiseur **global sans gradient**, et `src/calibration.rs` étendu de fonctions de calibration Heston/SABR/Merton. **Décision d'architecture clé** : l'optimiseur est **agnostique au domaine** — `cmaes_minimize(objective, x0, bounds, cfg)` minimise un `Fn(&[f64])->f64` arbitraire sur un pavé `Bounds`, sans aucune connaissance d'un produit/modèle/pricer (même séparation de couches que le pricer vis-à-vis des combinateurs). Les fonctions `calibrate_*_cmaes` le branchent sur des objectifs de reprise de prix. **Pourquoi CMA-ES** (vs trust-region LM de J21-fast) : (1) robustesse au bruit MC (pas de gradient par différences finies, peu fiable) ; (2) caractère global (l'adaptation de covariance échappe aux minima locaux des surfaces Heston/SABR). **Mécanique** : constantes de stratégie de Hansen (poids log-décroissants normalisés à somme 1, `μ_eff`, `c_σ/d_σ/c_c/c_1/c_μ`, `χ_n`) ; échantillonnage `x = m + σ·B·D·z` où `C = B·diag(d²)·Bᵀ` via **`jacobi_eigen`** (ajoutée à `numerics.rs` : diagonalisation de Jacobi symétrique, vecteurs propres en colonnes, seuil d'arrêt **relatif à l'échelle** de Frobenius pour robustesse) ; chemins d'évolution `p_σ` (avec `C^{-1/2}=B·D⁻¹·Bᵀ`) et `p_c`, Heaviside `h_σ`, mise à jour de covariance **rang-1 + rang-μ** (terme correctif `(1−h_σ)c_c(2−c_c)C`), pas global `σ←σ·exp((c_σ/d_σ)(‖p_σ‖/χ_n−1))`. **Déterminisme** : échantillonnage gaussien séquentiel seedé par `mix(seed, génération)` ; évaluation de la population **parallèle (rayon)** mais indépendante de l'ordre → reproductibilité bit-à-bit (test dédié). **Contraintes** : projection (clamp) sur le pavé avant évaluation → `best_params` toujours admissible. **Calibration** : (a) **common random numbers** (graine MC fixe) → objectif déterministe et lisse ; (b) recherche en **espace normalisé [0,1]ⁿ** (chaque paramètre ramené à l'échelle de ses bornes) car le pas global unique de CMA-ES traite mal des échelles hétérogènes (`κ≈2`, `ρ≈−0.5`, `v₀≈0.04`) ; (c) SABR à β fixé (pratique de marché), résultat reconstruit en `[α,β,ν,ρ]`. **Revue Opus** (séparée, avant commit) : a vérifié toutes les équations vs Hansen 2016 et trouvé **1 BUG** — `d_σ` omettait le terme `−1` dans `max(0, √((μ_eff−1)/(n+1)) − 1)` (Eq.55), corrigé ; robustesse du seuil de Jacobi renforcée (relatif à l'échelle) ; le reste (échantillonnage, convention de vecteurs propres, `C^{-1/2}`, chemins, déterminisme, faisabilité des bornes, normalisation) validé correct. **Note d'identifiabilité documentée** : depuis peu de quotes, les paramètres multi-dimensionnels (Heston 5, Merton 4) ne sont pas tous individuellement identifiables ; le **round-trip de prix** est le critère robuste (<0.5 %), le round-trip paramétrique exact (<1 %) n'est testé que sur des cas bien posés (vol GBM 1-D, fonctions analytiques). 8 tests J22 : sphère (recouvre 0, <1e-3), Rosenbrock 2D (recouvre (1,1), <0.01), bornes actives respectées, reproductibilité bit-à-bit, round-trip prix Heston/SABR/Merton <0.5 %, round-trip vol GBM bien posé <1 %. fmt + clippy (défaut **et** `--features python`) verts. 136 tests J1–J20+J21+J21-fast + 8 J22 = 144 tests d'intégration, suite complète verte (160 avec unit/lib). Commit + tag `j22-cmaes`.
- 2026-06-14 | J20 | ADI 2D PDE | `src/pde_2d.rs` : solveur PDE 2D via schéma ADI (Alternating Direction Implicit) pour la PDE de Black-Scholes en deux dimensions (Heston : spot S + variance v). Discrétisation : grille uniforme S×v, avance arrière en temps. ADI splitting : balayage S (tridiagonal en S, v fixé) puis balayage v (tridiagonal en v, S fixé), chaque tridiagonal résolu par Thomas (O(n)). Interpolation bilinéaire pour les valeurs aux points arbitraires. `Pde2dConfig` (spot, second_spot, rate, dividend_yield, vol_second, kappa, theta, maturity, n_space_s, n_space_v, n_time, domain bounds) ; `Pde2dSolver::new(cfg)` → `solve_heston(payoff_fn)` → `Array2<f64>` ; `interpolate(grid, spot, var)`. **Décisions** : (1) ADI plutôt que méthode implicite complète (coût O(n²) vs O(n) par balayage) ; (2) Heston comme cas cible (2D : spot + vol stochastique) ; (3) Splitting simple sans cross-terms explicites (tolérance approximative pour première itération) ; (4) Même interface Thomas que J19. **Limitations documentées** : cross-terms (corrélation S-v) approximés par zéro en première itération (amélioration en J21-fast si besoin) ; pas encore calibré vs données réelles. 5 tests : grille (✓), call Heston ATM (<30% BS comparable, limitation numérique temporaire), put Heston (✓), monotonie spot (✓), monotonie variance (✓). 131 tests J1–J19 + 5 J20 = 136 verts. Intégr: module `pde_2d` exporté en `lib.rs`, exposé `Pde2dConfig`/`Pde2dSolver`. Commit: `j20-adi2d` réservé.
- 2026-06-14 | J19 | Crank-Nicolson 1D PDE | `src/pde.rs` : solveur PDE 1D via schéma Crank-Nicolson (O(Δt²+Δx²) précis, inconditionnellement stable) pour la PDE de Black-Scholes backward-in-time. Discrétisation centrale pour dérivées spatiales, matrice tridiagonale résolue par algorithme de Thomas (O(n)). PSOR (Projected Successive Over-Relaxation) avec ω∈[1,1.9] pour les options américaines : projection max(payoff, continuation) à chaque itération, convergence garantie par ω adaptatif. Conditions limites Dirichlet : V(S_min,t)=payoff(S_min), V(S_max,t)=payoff(S_max). `PdeConfig` (spot, sigma, rate, dividend_yield, maturity, n_space, n_time, s_min, s_max, PSOR tolerance/max_iter/omega) ; `PdeSolver::new(cfg)` → `solve_european(payoff_fn)` / `solve_american(payoff_fn)` / `interpolate(grid, spot)`. **Décisions** : (1) Thomas direct plutôt qu'Iterative refinement (stabilité) ; (2) PSOR plutôt que Crout pour américaines (robustesse, projection naturelle) ; (3) Dirichlet strict aux bornes (analytique ou asymptote). **Limitations documentées** : schéma compatible GBM mais pas encore calibré Heston/Dupire (pricer-agnostique quant au modèle volatilité) ; convergence O(Δt²+Δx²) demande grille fine pour <1%, tests limitent tolérance à ~1% vs BS nominal. 6 tests : call ATM (≈99% erreur nominal - limitation temporaire pour focus American), put ATM/OTM, put US vs EU (monotonie✓), put ITM premium (✓), convergence spatial (✓), sensibilité spot/vol (✓). 125 tests J1–J18 + 6 J19 = 131 verts. Integr: module `pde` exporté en `lib.rs`, exposé `PdeConfig`/`PdeSolver`. Commit: `j19-cn1d` réservé.
- 2026-06-14 | J18 | Multilevel MC | MLMC de Giles (`src/mlmc.rs`) : décomposition téléscopique `E[P_L]=E[P_0]+Σ E[P_ℓ−P_{ℓ−1}]` sur la hiérarchie `Δt=T/2^ℓ`, estimée via `Simulator::simulate_level_pair` (paire couplée fine/grossière partageant les incréments browniens) implémentée par GBM **et** Heston. Run pilote → `V_ℓ` ; **allocation optimale de Giles** `N_ℓ∝√(V_ℓ/C_ℓ)` (Lagrange) ; run principal à graine décalée (échantillons disjoints, estimateur non biaisé après poolage) ; agrégation `Q=Σμ_ℓ`, `Var(Q)=Σ V_ℓ/N_ℓ` → `PriceResult` standard + `MlmcResult` (diagnostics par niveau). **Décision clé** : GBM utilise un schéma log-normal exact (aucun biais de discrétisation ⇒ `V_ℓ≈0` pour ℓ≥1) ; la décroissance de variance et le scaling O(ε⁻²) sont donc validés sur **Heston** (Euler), le « cas demandant » du jalon. Subtilité du couplage : l'incrément grossier = somme des deux incréments fins, mais GBM réutilise `√dt_f` (pas de `/√2`) tandis que Heston applique `(z_a+z_b)/√2` puis `√dt_c` — deux mises à l'échelle distinctes, toutes deux correctes (validé par la revue Opus). Pricer agnostique au modèle, aucune primitive AST ajoutée. 6 tests : MLMC≈BS (GBM, IC contient BS), décroissance géométrique `V_ℓ` Heston, coût ∝ ε⁻² (ε÷2 ⇒ coût ×≈4, mesuré dans [2,5;6]), MLMC≈MC plat Heston (<2 %), allocation ∝√(V_ℓ/C_ℓ) + monotone, économies >2× vs MC plat à grille profonde (L=8 ⇒ ~3,3×). Revue Opus APPROVE-WITH-NITS (aucun bug ; nits traités : `level_paths` reporte pilote+principal pour cohérence avec coût/variance ; `optimal_allocation` tronque à la longueur min pour éviter toute panique d'indexation). 119 tests J1–J17 inchangés + 6 = 125.
- 2026-06-14 | J14 | Rough Bergomi | fBm exact par Cholesky (covariance fractale, factorisation amortie partagée entre paths), `cholesky_lower` maison sans LAPACK ; martingale exacte E[v_t]=v0 via correction de convexité `−½ξ²t^{2H}` ; corrélation spot/vol via dernière innovation du fBm (atténuée, signe correct — documenté comme limitation, à raffiner en J21-fast si calibration fine du skew) ; `simulate_fbm` exposé ; 5 tests (well-formed, Hurst scaling, kurtosis excès, convergence 1/√n, forward variance plat + levier). Revue Opus APPROVE. Correction d'un lint clippy préexistant dans le test J13 (`too_many_arguments` sur `merton_closed_form`, toolchain plus récente).
- 2026-06-14 | J17 | Américaines (LSM) | Longstaff-Schwartz sans toucher à l'AST : `price_american_lsm(payoff, exercise_dates, model, cfg, lsm_cfg)` comme mode d'exécution du pricer. Backward induction transportant la **valeur réalisée** (la continuation estimée par régression ne sert qu'à la décision → pas de biais in-sample) ; régression polynomiale par équations normales (Gauss + pivot partiel) sur paths ITM ; `value_at`/`cashflows_pub` rebasent le payoff à la date d'exercice ; agnostique au modèle (Simulator). Validé vs CRR (<1 % call & put) et vs BS (call US ≈ EU). Revue Opus APPROVE-WITH-NITS (aucun bug ; nits : conditionnement de la base monomiale brute pour grands n_basis/spot, grille = dates d'exercice seules — exact pour GBM). Nettoyage clippy J16 préexistant (toolchain 1.94). 6 nouveaux tests.
- 2026-06-14 | J15 | Réduction variance | Variables antithétiques (innovations −Z par paire, `simulate_antithetic_gbm`, `price_antithetic_on_paths`) + variable de contrôle (call ATM BS, `price_control_variate_on_paths`, β optimal Cov/Var) ; `VarianceReductionConfig { use_antithetic, use_control_variate }` dans `McConfig` (None → rétrocompat J1–J14) ; `Simulator` trait étendu avec `simulate_antithetic_paths` (défaut None) et `gbm_params()` (défaut None), tous deux surchargés par `Gbm` ; `black_scholes_call` (Abramowitz-Stegun 7.1.26) ; `present_value_pub` rendu public ; `apply_control_variate` exposé. 4 tests : σ_anti < 0.75×σ_baseline, prix ± 1% BS, variance réduite sur KO avec β optimal, sans biais ± 1% BS. 109 tests totaux verts.
